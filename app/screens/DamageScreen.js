import React,{useState,state} from 'react';
import {Card} from 'react-native-paper';
import { AppRegistry, StyleSheet,Modal, View, Dimensions, Alert, Text, Image,Button,FlatList,TouchableOpacity, Keyboard } from 'react-native';
import{MaterialIcons} from '@expo/vector-icons';
import ReviewForm from './reviewForm'
import { TouchableWithoutFeedback, State } from 'react-native-gesture-handler';
import {CircleButton} from "../components/circlebutton";
import styles from "../styles/damagescreen.styles";
import {Pick} from "../components/circlebutton";

export default function DamageScreen({navigation}) {
 
  


  return (
    <Pick/>

  );

}


DamageScreen.navigationOptions = {
 header:null

};
